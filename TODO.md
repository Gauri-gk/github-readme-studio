# Task TODO - Vite app deploy fix

- [ ] Identify deployment issue cause (Vercel/Render serving default Vite page)
- [ ] Verify entrypoint/index.html and Vite build input
- [ ] Check for accidental nested duplicate folders and wrong imports
- [ ] Confirm package.json scripts and Vite build output
- [ ] Run `npm run build` locally and verify dist contains app
- [ ] Update Vite/TypeScript configuration only as needed (no UI changes)
- [ ] Record exactly which files were changed

